# Email RAG Pipeline Environment Configuration

# OpenAI API Key (required for embeddings and generation)
OPENAI_API_KEY=your_openai_key_here

# PostgreSQL Database Connection
# The script automatically loads the root .env file from the parent directory,
# so it will use the same database credentials as other Steelhead services.
# You only need to set DATABASE_URL here if you want to override the default.
#
# The following variables are automatically sourced from the root .env:
# - ROOT_USER_PASSWORD (used as PASSWORD_POSTGRES default)
# - EXPOSE_POSTGRES (used as PORT_POSTGRES default)
# - HOST_POSTGRES, PORT_POSTGRES, CONFIG_STORAGE, USER_POSTGRES, PASSWORD_POSTGRES
#
# DATABASE_URL=postgresql://steelhead:password@localhost:10001/steelhead

# Override individual connection parameters (optional - defaults from root .env)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=10001
# POSTGRES_DB=steelhead
# POSTGRES_USER=steelhead
# POSTGRES_PASSWORD=from_root_env

# RAG Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K=5

# LLM Configuration
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.0

# API Configuration
API_HOST=0.0.0.0
API_PORT=8001

# Optional: LangChain tracing
# LANGCHAIN_API_KEY=your_langchain_key
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=email-rag
